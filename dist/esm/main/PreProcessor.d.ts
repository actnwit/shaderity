export default class PreProcessor {
    private static definitions;
    private static evaluateCondition;
    static process(splittedLines: string[], startLine?: number, endLine?: number): string[];
}
